{% extends "templates/base.njk" %}
{% import "templates/post.njk" as blogPost %}

{% block main %}
    {% for post in posts %}
        {{ blogPost.post(post.title, post.id, post.niceTime) }}
    {% endfor %}
{% endblock %}